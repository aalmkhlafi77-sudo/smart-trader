# بنية تطبيق التاجر الذكي

## المبدأ الأساسي

النسخة الحالية تعمل كتطبيق PWA بتخزين محلي بالكامل داخل جهاز المستخدم، دون خادم أو قاعدة بيانات سحابية. ومع ذلك، صُممت البنية لتكون قابلة للتحويل لاحقاً إلى تطبيق مغلف يعتمد على SQLite دون إعادة كتابة الواجهة.

## طبقات التطبيق

```
الواجهة (الصفحات والمكونات)
        ↓
طبقة الخدمات (services)
        ↓
واجهة مستودع البيانات (Repository Interface)
        ↓
محرك التخزين (IndexedDB الآن، SQLite لاحقاً)
```

الواجهة لا تتعامل مع محرك التخزين مباشرة. أي شاشة تستدعي دوال مثل `listItems` أو `savePricing`، ولا تعرف نوع قاعدة البيانات المستخدمة.

## قواعد التوافق مع SQLite

1. كل كيان يُخزَّن كسجل مسطح بحقول واضحة، لا ككائن متداخل غير منظم.
2. المفاتيح نصية ثابتة (`id`) وتُولَّد داخل التطبيق.
3. العلاقات تُمثَّل بمفاتيح خارجية صريحة مثل `entityId` و`itemId`.
4. القيم المالية تُحفظ كأرقام، والتواريخ تُحفظ بصيغة ISO نصية.
5. كل جدول يملك `createdAt` و`updatedAt`.
6. يوجد جدول `meta` يحفظ رقم إصدار المخطط لتشغيل الترقيات.
7. لا تُخزَّن الصور داخل السجل نفسه، بل يُخزَّن مرجع للصورة في جدول منفصل.

## الجداول

| الجدول | الوصف |
|---|---|
| entities | الجهات: موردون وعملاء ومصانع |
| items | الأصناف الأساسية |
| item_specs | مواصفات الصنف المرنة |
| item_prices | أربعة أسعار يدوية مع تاريخ اعتماد لكل سعر |
| pricing_runs | عمليات حساب تكلفة الحاوية والتسعير |
| price_lists | قوائم الأسعار المعدة للتصدير |
| price_list_items | عناصر قائمة الأسعار |
| settings | إعدادات التطبيق والعملة المحلية |
| meta | إصدار المخطط وبيانات النسخ الاحتياطي |

## معادلة التكلفة المعتمدة

```
قيمة البضاعة = سعر الوحدة المختار × الكمية × سعر الصرف
الجمارك = قيمة البضاعة × نسبة الجمارك
إجمالي تكلفة الحاوية = قيمة البضاعة + الشحن + الجمارك + التخليص + النقل + مصاريف أخرى
تكلفة الوحدة = إجمالي تكلفة الحاوية ÷ الكمية
سعر البيع = تكلفة الوحدة × (1 + هامش الربح)
```

جميع المصاريف المقطوعة تُحسب على مستوى الحاوية أولاً، ثم تُقسَّم على الكمية.

## مسار التحويل إلى SQLite

1. إضافة تنفيذ جديد لواجهة المستودع باسم `sqliteRepository`.
2. تغليف التطبيق باستخدام Capacitor.
3. تصدير نسخة احتياطية من النسخة الحالية بصيغة JSON.
4. استيراد النسخة داخل قاعدة SQLite عبر نفس أسماء الحقول.
5. التحقق من تطابق عدد السجلات في كل جدول.

لا تحتاج الشاشات ولا معادلات الحساب إلى تعديل في هذا المسار.

