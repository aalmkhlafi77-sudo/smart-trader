# تقرير التدقيق الأمني وتجهيز GitHub

**المشروع:** التاجر الذكي (`smart-trader`)  
**نطاق العمل:** نسخة مستقلة في `smart-trader-github-clean` فقط، من دون رفع إلى GitHub ومن دون تعديل مشروع العمل الأصلي.  
**تاريخ الفحص:** 2026-08-23

> تم الحفاظ على واجهة التطبيق، وتدفقات إدارة الجهات والأصناف والأسعار، ومعادلات التكلفة والتسعير، ومنطق التقارير. اقتصرت التغييرات الوظيفية على إزالة أدوات تشغيل غير مستخدمة، وتوطين الأصول، وإصلاح تشغيل خادم الإنتاج.

## النتيجة التنفيذية

أصبحت النسخة قابلة للتثبيت والتشغيل محلياً كـ React/Vite/IndexedDB مستقل، من دون وكيل تخزين أو تحليلات أو وقت تشغيل خاص بالمنصة. أزيلت التبعيات غير المستدعاة، وحُدِّثت تبعيات الإنتاج والتطوير بصورة متوافقة، وانتهى **تدقيق الإنتاج والتدقيق الكامل معاً** إلى **0 ثغرات حرجة و0 عالية و0 متوسطة و0 منخفضة**. لا تتضمن الحزمة ملفات تشغيل أو إعدادات بيئية أو سجلات أو بيانات اعتماد.

| المعيار | قبل المعالجة | النتيجة النهائية |
| --- | ---: | ---: |
| تدقيق الإنتاج `pnpm audit --prod` | 0 حرجة، 18 عالية، 47 متوسطة، 8 منخفضة | 0 في جميع الدرجات، رمز الخروج 0 |
| التدقيق الكامل `pnpm audit` | 2 حرجة، 18 عالية، 14 متوسطة، 1 منخفضة | 0 في جميع الدرجات، رمز الخروج 0 |

## ما تغيّر

| المجال | الإجراء | الأثر المقصود |
| --- | --- | --- |
| التشغيل والخصوصية | أزيلت أداة جمع سجلات المتصفح، ووكيل التخزين الخارجي، ووقت التشغيل الخاص بالمنصة، وسكربت التحليلات. | منع إرسال السجلات أو طلب الأصول أو التحليلات عبر خدمات خارجية. |
| الأصول | وُطّنت صورة الخلفية وصورة لوحة العمل وشعار التطبيق في `client/public/assets/` بصيغة WebP، وربطت الواجهة وملف PWA وتقارير PDF بهذه المسارات. | بقاء المظهر كما هو مع العمل دون وكيل تخزين. |
| ملفات موروثة | حُذفت مكوّنات `ManusDialog.tsx` و`Map.tsx` و`ui/chart.tsx` و`template.json` بعد التحقق من عدم وجود أي استيراد أو استدعاء لها. | تقليص سطح الشفرة والتبعيات غير المستخدمة من دون حذف وظيفة مستدعاة. |
| خادم الإنتاج | أُبقي خادم Express لخدمة `dist/public` وأصلح استيراد Express المكرر الذي اكتشفه فحص الأنواع. | ضمان عمل `pnpm start` في الإنتاج. |
| حزم غير مستخدمة | أزيلت `axios` و`streamdown`، إضافة إلى `recharts` و`nanoid` وحزم التطوير الخاصة بالخريطة ووقت التشغيل غير المستخدم. | إزالة سلاسل تبعيات تضمنت عدداً كبيراً من التنبيهات. |
| حزم محدّثة | حدّث `express` إلى `4.22.2`، وثبّت `path-to-regexp` على `0.1.13` ضمن نطاق Express المتوافق. | معالجة GHSA-37ch-88jc-xwx2. |
| تصدير Excel | استُبدلت `xlsx@0.18.5` بالحزمة الرسمية `xlsx@0.20.3` من CDN SheetJS. | معالجة تنبيهي SheetJS عاليي الخطورة مع الحفاظ على API التصدير. |
| أدوات التطوير | حُدّثت `vite` إلى `7.3.6` و`vitest` إلى `3.2.7` و`@vitejs/plugin-react` إلى `5.2.0` وTailwind إلى `4.3.3`. كما حُدّثت الشجرة المتداخلة إلى `postcss@8.5.26` و`rollup@4.62.5` و`nanoid@3.3.18` و`picomatch@4.0.5` و`@babel/core@7.29.7`. | إزالة ثغرات أدوات البناء والاختبار من دون استخدام إصلاح قسري. |
| الخطوط | وُطّنت أوزان Cairo وIBM Plex Sans Arabic المستخدمة في الواجهة وتقارير PDF داخل `client/public/fonts/`. | بقاء الهوية الطباعية مع منع أي طلب إلى Google Fonts وقت التشغيل. |
| بقايا OAuth | حُذف `client/src/const.ts` غير المستدعى بعد التحقق من عدم وجود استيراد له. | إزالة مراجع `VITE_OAUTH_PORTAL_URL` و`VITE_APP_ID` الموروثة. |
| التوثيق | أضيف `README.md` للتثبيت والتشغيل والاختبارات والنسخ الاحتياطي. | تجهيز المستودع للمراجعة والاستخدام المستقل. |

## الاعتمادات والأمان

تم تحديث ملف القفل باستخدام **pnpm 10.4.1**، ثم تحقق التثبيت بـ`pnpm install --frozen-lockfile`. لا استُخدم `pnpm audit --fix --force` أو أي إصلاح آلي قد يغير الشجرة دون مراجعة. احتُفظ بخط أساس الإنتاج في `audit-before.json`، وبالنتائج النهائية في `audit-full-after.json` و`audit-prod-after.json`.

| البند | الحالة النهائية | المرجع |
| --- | --- | --- |
| SheetJS | `0.20.3` من الحزمة الرسمية | [1] |
| Express | `4.22.2` | [2] |
| path-to-regexp | `0.1.13` مثبت صراحةً ومتوافق مع نطاق Express `~0.1.12` | [3] |
| أدوات البناء | Vite `7.3.6` وVitest `3.2.7` مع تجاوزات متوافقة للتبعيات المتداخلة المتأثرة | [4] |
| تدقيق pnpm | صفر تنبيهات على جميع التبعيات وعلى تبعيات الإنتاج | [4] |

## التحقق المنفذ

| الفحص | النتيجة |
| --- | --- |
| `pnpm install --frozen-lockfile` | ناجح باستخدام pnpm 10.4.1. |
| `pnpm test` | 7 اختبارات ناجحة في 4 ملفات، وتشمل التكلفة والتسعير والسعر المؤرخ والتقارير وتصدير PDF. |
| `pnpm run check` | ناجح؛ لا أخطاء TypeScript. |
| `pnpm run build` | ناجح؛ بنيت واجهة الإنتاج وخادم Express. |
| `pnpm start` | ناجح على منفذ محلي مستقل، مع استجابة HTTP 200 للأصل والشعار وصورة لوحة العمل. |
| `pnpm audit` | ناجح؛ 0 حرجة و0 عالية و0 متوسطة و0 منخفضة. |
| `pnpm audit --prod` | ناجح؛ 0 حرجة و0 عالية و0 متوسطة و0 منخفضة. |
| فحص الواجهة اليدوي | نجح فتح القفل المحلي، والتنقل إلى لوحة العمل وحاسبة التكلفة والتقارير والنسخ الاحتياطي. |
| فحص Excel | أُنشئ مصنف `xlsx` مؤقت بالعنوان العربي **«تقرير التكلفة»**، ثم أُعيدت قراءته بنجاح مع النص العربي والقيمة الرقمية `27000` والتاريخ `2026-08-23`. حُذفت ملفات الفحص بعد التحقق. |
| فحص الشبكة | لم تسجل الموارد المحمّلة أي عنوان يتضمن `manus` أو `forge` أو `umami` أو `analytics` أو `fonts.googleapis.com` أو `fonts.gstatic.com`. |
| فحص الأسرار | لم تظهر ملفات مصدر تحوي مفاتيح أو رموزاً مضمنة. النتيجة الوحيدة كانت حقول إدخال رمز PIN المحلي في `AccessSettingsCard.tsx`، وهي لا تتضمن قيمة ثابتة أو بيانات اعتماد. |

## الاستبعادات المطلوبة للحزمة وGit

يستبعد `.gitignore` صراحةً مجلدات التبعيات والبناء والسجلات وإعدادات التشغيل: `node_modules/` و`dist/` و`.manus/` و`.manus-logs/` و`.project-config.json` و`.env*` و`client/public/__manus__/`. كما لا يوجد في النسخة النهائية أي مرجع إلى `/manus-storage` أو متغيرات Forge أو التحليلات أو نطاقات المنصة.

لا يحتاج التطبيق المحلي المستقل إلى متغيرات تشغيل. لا يوجد ملف `.env` أو أي قيمة سرية في الحزمة؛ يكفي تحديد `PORT` اختيارياً عند الحاجة لتغيير منفذ خادم الإنتاج.

## طريقة التشغيل بعد الرفع

```bash
corepack enable
pnpm install --frozen-lockfile
pnpm test
pnpm run check
pnpm run build
pnpm start
```

يخزّن التطبيق بياناته في IndexedDB داخل متصفح المستخدم. قبل استخدامه الفعلي، ينصح بإنشاء نسخة احتياطية من صفحة **النسخ الاحتياطي** وحفظها خارج المتصفح.

## المراجع

[1]: https://cdn.sheetjs.com/ "CDN الرسمي لـ SheetJS"
[2]: https://www.npmjs.com/package/express/v/4.22.2 "Express 4.22.2"
[3]: https://github.com/advisories/GHSA-37ch-88jc-xwx2 "GHSA-37ch-88jc-xwx2"
[4]: https://pnpm.io/10.x/cli/audit "توثيق pnpm audit"
